RFE PROTOTYPE TEST-BED — PROJECT HANDOVER

1. PROJECT STATE

State:
- APPROVED THROUGH: Milestone 13 (M12–M13 hard-save publication authorised 2026-08-15).
- QUALIFIED BATCH: M10 / M10.1 / M11 / M12 / M13, including accepted browser-regression remediation and diagnostic history.
- Authoritative branch: `milestone/ui-interaction-harness`.
- Qualified M12–M13 implementation commit: `3be2f6e9f94b6946facefc60825559fcefb56b87`; GitHub merge commit: `cec46def328b8341f87222b84ffc903eac1fb7bc`.
- Previous approved M09: `9af3b8d5d64f8a1bdfbb34f01828c2003a24b314`, preserved in ancestry.
- Historical review branch: `review/m10-safety-audit` at the qualified implementation commit; it is review evidence, not development authority.
- Current development phase: M13 musical-source architecture, generator expansion, recording-safe Multi-Plot delivery, and cluster workflow are user-accepted on `agent/milestone-13` and authorised for promotion to the authoritative branch.
- Handover updated: 2026-08-15 (Europe/London).

Product/runtime authority:
- PRIMARY PRODUCT TARGET: one locally launchable offline desktop RFE Test Engine.
- SECONDARY TARGET: a future Tablet Compact version derived from the same authoritative project lineage.
- CURRENT BROWSER BUILD: development/prototyping runtime only; Safari/WebKit is not the architectural authority for the final audio engine.
- NEXT DEVELOPMENT PHASE: await the explicit M14 brief. Do not infer or begin later roadmap work from the M12–M13 acceptance.
- Do not create Safari-, Chromium-, desktop-, or tablet-specific duplicate project engines. A future hosted/browser product would require its own separately approved architecture.

The accepted local audio configuration uses native Web Audio compression/sample fencing plus a colour-coded layered endpoint waveform at a bounded 30 Hz visual rate. The custom AudioWorklet implementation is preserved, but Safari testing isolated it as the source of progressive crackle/latency and delayed transport. Worklet master-meter reporting, non-finite latching and the eventual offline desktop safety boundary are deferred for a later explicit decision. Channel deletion now uses a non-blocking inline confirmation and preserves unrelated sounding channels.

The application is a local RFE research-interface prototype with three ordered workspaces: Threads, Sound Desk and Visualiser. Threads programs dynamic isolated sine channels and their base levels. Place transfers an unplaced player into the construction canvas as one stable channel-bound output-only source node; it can then be explicitly wired to any free Channel Out. The full player moves from the sidebar to Inspector while placed, preventing duplicate menu clutter. Disconnecting Channel Out smoothly silences only that endpoint while preserving its source and programmed state; reconnecting restores its prior trim. Sound Desk owns canonical X/Y placement and bounded relative Live Trim; X drives each endpoint's native stereo panner while Y remains acoustically neutral. Threads, Sound Desk and Visualiser share Play/Pause/Stop control of one session gate and elapsed timer. Monitor keeps selected-channel context while rendering one colour-coded analyser trace per sounding endpoint, and reports `SD MUTED` rather than `NO SIGNAL` when the focused active route is muted at -100% in Sound Desk.

Milestones 06–09 have passed user listening/interaction acceptance. Milestone 09 follows the explicit attached brief, is fully verified automatically, and was explicitly approved on 2026-08-12 as the new project baseline. Do not begin Milestone 10 without explicit instruction.

2. APPROVED BASELINE

Milestone 06 — Threads optimisation and audio test-bed preparation:
- Threads defaults to one routable channel, CH 01, rather than a five-channel fixture.
- Channels are reducer-owned, dynamically addable/removable and not capped at five.
- Channel identities are derived from active state: when none remain, the next channel is CH 01; otherwise the next is one above the greatest active number. Example: active 01/05/09 produces 10. Active IDs never duplicate.
- The Inputs & Channels sidebar contains a dedicated scroll region. Large channel populations retain full-height rows and reachable remove controls without stretching or breaking the workspace.
- Removing a channel removes only its terminal and downstream spatial assignment; surviving channel identities and routes remain stable.
- Threads retains explicit output-terminal connectivity and derived complete/incomplete status.
- The construction workspace, Inspector, tool expansion, Monitor, Sound Desk routing and Visualiser observation remain compatible with dynamic channels.
- Monitor is contextual and no longer presents placeholder signal truth when real CH 01 data exists.

Milestone 07 — single-channel audible validation:
- CH 01 produces one native Web Audio sine oscillator through a conservative channel gain and shared application output.
- Frequency range is 40–2000 Hz, default 78 Hz, displayed in Hz.
- The slider supports quick sweeps; dedicated minus/plus controls provide exact 1 Hz steps and disable at the range boundaries.
- Level defaults to 20% and uses a compact rotary/hairpin-style accessible control.
- Start/Stop is explicit, keyboard accessible and user-gesture initiated. No autoplay occurs.
- Parameter and source-envelope changes use short ramps to reduce abrupt discontinuities.
- Monitor reports real CH 01 waveform, frequency, level and ACTIVE/SILENT state from the authoritative audio runtime.
- The waveform is derived from analyser time-domain data, renders only while active/visible and returns to NO SIGNAL when stopped.
- Audio intentionally continues during Threads -> Sound Desk -> Visualiser -> Threads navigation. Navigation does not duplicate the source or reconstruct shared output.
- CH 01 remains centred. X/Y routing has no audio meaning; there is no panning or spatialisation.
- User acceptance confirmed smooth operation after precision-control, channel-removal and channel-numbering fixes.

Retained approved Milestones 03–05 behavior:
- Ordered adjacent-only Threads -> Sound Desk -> Visualiser navigation with preserved shell state and reduced-motion handling.
- One canonical Cartesian 5 × 5 spatial definition shared by Sound Desk and Visualiser.
- Threads-owned channel existence/terminal connectivity and Sound Desk-owned optional coordinate assignments.
- Canonical reducer state for modules, Threads, selection, parameters, positions, canvas controls, workspace width and elapsed session.
- Editable module graph, validated connections, cleanup invariants, Inspector controls, Studio/Compact layouts and UI Workshop boundary.
- Browser accessibility gate with no serious/critical axe violations.

3. AUDIO OWNERSHIP MODEL

Authoritative runtime:
- `app/audio-runtime.ts` exports the module-level `applicationAudioRuntime` singleton.
- Audio lifecycle correctness does not depend on a React component remaining mounted.

Application-level ownership:
- One lazily created AudioContext/runtime.
- One shared master gain connected once to the AudioContext destination.
- Shared lifecycle coordination and explicit application shutdown.
- Master/runtime diagnostic counters and runtime error state.

Channel-level ownership:
- Oscillator/source.
- Short source envelope.
- Programmed channel gain and bounded relative Live Trim gain.
- Channel analyser.
- Native stereo panner driven only by canonical X.
- Channel parameters, status and scoped cleanup.

Graph:
`CHANNEL SOURCE -> SOURCE ENVELOPE -> PROGRAMMED GAIN -> LIVE TRIM -> ANALYSER -> STEREO PANNER -> SHARED SESSION GATE -> SHARED MASTER -> DESTINATION`

Lifecycle contracts:
- Changing frequency or level never recreates the master, channel graph or oscillator.
- Starting an already-active channel is idempotent.
- Stop disposes only the current oscillator/envelope and leaves reusable channel processing plus shared output intact.
- Restart creates exactly one new channel source.
- Channel disposal never closes the AudioContext or replaces/disconnects shared output.
- Only explicit application shutdown may close shared infrastructure.
- UI observer remount/navigation does not reconstruct the master graph.
- Initialisation errors are visible and retryable without crashing unrelated workspaces.

This isolation model now supports CH 02, CH 03 and later dynamically created concurrent instances. The five-channel validation fixture is a test count, not a maximum.

4. CURRENT ARCHITECTURE

- `app/audio-runtime.ts`: shared application audio runtime, isolated channel graphs, Web Audio lifecycle, analyser access, state subscription and diagnostics.
- `app/interaction-state.ts`: canonical Threads model, including active channel allocation, add/remove cleanup and restored-state sanitation.
- `app/channel-routing.ts`: channel definitions, terminal/source records, derived availability and terminal validation.
- `app/spatial-routing.ts`: canonical spatial coordinates and channel-owned optional routing assignments.
- `app/workspace-navigation.ts`: ordered workspace destinations and adjacent transition helpers.
- `app/page.tsx`: application shell, transferable per-channel sine controls/source nodes, contextual Monitor/analyser canvas, Sound Desk and Visualiser.
- `app/globals.css`: studio/window layout, scroll-contained dynamic channel list, audio controls, waveform and responsive behavior.
- `app/ui-config.ts` / `app/ui-workshop.tsx`: approved visual configuration and development-only tuning boundary.
- `tests/audio-runtime.test.mjs`: fake-Web-Audio lifecycle/ownership/error tests.
- `tests/channel-routing.test.mjs`: dynamic identity, active-state allocation, terminal and removal tests.
- `tests/interaction-state.test.mjs`, `tests/spatial-routing.test.mjs`, `tests/workspace-navigation.test.mjs`, `tests/ui-config.test.mjs`: retained model regression coverage.
- `tests/rendered-html.test.mjs`: built-worker structural and accessible-control checks.
- `e2e/ui-quality-gate.spec.ts`: real-browser UI, navigation, audio lifecycle, 24-channel scrolling/removal, numbering reset, accessibility and regression journeys.
- `milestone_notes/milestone_06_threads_optimisation.txt`: Milestone 06 implementation record.
- `milestone_notes/milestone_07_single_channel_audio.txt`: Milestone 07 ownership, verification and acceptance record.

Primary audio data flow:
1. UI reads a subscribed snapshot from `applicationAudioRuntime`.
2. Explicit Start creates/resumes shared infrastructure if required, then creates only the addressed channel source.
3. Parameter controls update only the addressed channel AudioParams with smoothing.
4. The focused channel's analyser provides real time-domain samples to Monitor while active.
5. Stop removes the source and halts waveform rendering; shared infrastructure remains stable.
6. Navigation remounts observers where appropriate but does not own or rebuild the audio engine.

5. FILES CHANGED IN THE APPROVED 06–07 BATCH

- `app/audio-runtime.ts` (new): isolated Web Audio runtime and diagnostics.
- `app/channel-routing.ts`: one-channel/dynamic-channel routing model updates.
- `app/interaction-state.ts`: dynamic channel lifecycle, cleanup, active-state numbering and restoration behavior.
- `app/spatial-routing.ts`: dynamic channel synchronization and retained assignments.
- `app/page.tsx`: optimised Threads test-bed, dynamic channel controls, CH 01 audio controls and real Monitor.
- `app/globals.css`: revised Threads density/sidebar, scroll containment, audio controls and waveform.
- `tests/audio-runtime.test.mjs` (new): audio ownership and lifecycle coverage.
- `tests/channel-routing.test.mjs`: dynamic population, removal and numbering coverage.
- `tests/rendered-html.test.mjs`, `tests/spatial-routing.test.mjs`: structural/regression updates.
- `e2e/ui-quality-gate.spec.ts`: Milestone 06–07 browser acceptance and regression journeys.
- `milestone_notes/milestone_06_threads_optimisation.txt` (new).
- `milestone_notes/milestone_07_single_channel_audio.txt` (new).
- `HANDOVER.txt`, `QUALITY_GATE.md`, `README.md`: repository truth advanced through Milestone 09.

No application files were removed.

6. IMPORTANT IMPLEMENTATION DECISIONS

- Application audio infrastructure and channel audio instances have separate ownership. Do not move AudioContext creation into a transient component.
- Do not close/reset shared audio resources during channel Stop, parameter changes, channel disposal or UI remount.
- Future audible channels must receive independent source/gain/analyser ownership and connect into the existing shared master.
- Audio is local/browser-native. No remote processing, cloud audio service or server-side audio dependency exists.
- Conservative gain staging is intentional: channel maximum 0.2, shared master 0.8 and default level 20%.
- There is no limiter/mastering chain. Start manual listening at conservative device volume.
- Workspace navigation intentionally permits active channels to continue. Changing this policy requires an explicit product decision and lifecycle tests.
- Spatial routing and audio generation remain separate. Coordinates are retained data but do not pan or process sound.
- Dynamic channel numbering is based on active channel state, not a permanent lifetime counter. Gaps below the current maximum are not reused while channels remain.
- Channel list scrolling is internal to Inputs & Channels. Do not allow flex/grid shrink to clip fixture content or controls.
- AppState remains the source of truth for application interaction. Audio runtime state is engine state and must not be duplicated into reducer/UI state.
- Visual configuration remains separate from both AppState and audio-engine state.
- Existing cleanup invariants, stable identity, spatial ownership and navigation contracts remain in force.

7. DO-NOT-BREAK CONTRACTS

- One active channel source must never recreate or replace shared output.
- Stop must silence/dispose the channel source; Restart must return exactly one source.
- Channel disposal must remain scoped and must never close the application AudioContext.
- Frequency/level updates must target the current source/channel graph without parallel oscillator creation.
- Monitor must reflect actual active state, frequency and level; the waveform must use analyser data rather than decorative animation.
- No autoplay. Audio initialization/resume must follow an explicit user gesture.
- Preserve accessible Start/Stop, frequency slider, exact frequency buttons, level control and text state.
- Preserve active-state channel numbering: empty -> 01; otherwise max(active)+1; never duplicate an active ID.
- Preserve full-height, independently scrollable dynamic channel rows and reachable remove controls at large populations.
- Removing a channel must remove only its own terminal/spatial/audio resources and retain peers.
- Preserve reducer ownership, module/Thread distinction, connection validation and orphan cleanup.
- Preserve canonical spatial coordinates and Sound Desk/Visualiser shared routing truth. X alone drives pan; Y must remain acoustically neutral until explicitly scoped.
- Do not add distance/depth/elevation audio, automatic distribution, effects, recording, file playback or simulation without an explicit milestone brief.
- Maintain TypeScript, ESLint, production build, model/browser regression and axe gates.

8. KNOWN LIMITATIONS / DEFERRED WORK

- Every dynamic channel can own an audible sine instance; five-channel concurrency is the current validated test fixture rather than a capacity limit.
- Audible behavior depends on browser/device output. Automated tests prove lifecycle and analyser behavior, not human-perceived sound quality.
- Short ramps reduce likely clicks but device-specific clicks/pops remain a listening concern for future work.
- Basic X-to-stereo pan exists. There is no Y audio, distance/depth/elevation, HRTF, binaural/surround processing or automatic distribution engine.
- No live input, recording, sample/file playback, loops, effects, EQ, compression or mastering protection.
- No particle/field simulation or audio/visual gating.
- Persistence remains browser-local; there is no durable project database/collaboration contract.
- Free window management, product Focus/Favourites and deeper workflows remain deferred.
- Audio diagnostics are exposed for engineering validation but are intentionally not a full user-facing telemetry panel.

9. VERIFICATION

Run from the repository root with Node.js 22.13+ and pnpm 11.16.0:

1. `pnpm run typecheck`
   Expected: no TypeScript diagnostics.

2. `pnpm run lint`
   Expected: no ESLint errors.

3. `pnpm run build`
   Expected: vinext production build succeeds.

4. `node --experimental-strip-types --test tests/*.test.mjs`
   Expected at handover: 90/90 Node tests pass.

5. `pnpm exec playwright test`
   Expected at handover: 19/19 Playwright tests pass, including axe.

6. `pnpm run test:stress-ui`
   Expected: fixture validation and manual soak instructions.

Latest completed verification before repository reconciliation:
- Typecheck: PASS
- ESLint: PASS
- Production build: PASS
- Node tests: PASS, 90/90
- Playwright/browser tests: PASS, 19/19
- axe serious/critical scan: PASS
- Milestone 08 user audible/interaction acceptance: PASS
- Milestone 09 user audible/interaction acceptance: PASS
- Git diff integrity: PASS

Latest M12 first-listening remediation qualification (2026-08-14):
- Live Multi-Plot creation now connects its already-running source branch at zero gain and ramps it to the requested endpoint gain, removing the graph discontinuity responsible for the reported add-pop.
- Drag selects the nearest real coordinate-cell centre and shows a target engagement ring; the plot overlay now shares the grid's padded inner coordinate box, removing the increasing non-zero calibration error.
- A complete source family with no plotted endpoint has a Threads-only audition route so its programming can be trialled before going live spatially. That preview closes when leaving Threads or once any family endpoint is plotted, so it cannot escape Sound Desk mute; muting every plotted endpoint yields zero sounding endpoints.
- User subsequently completed an overload session beyond a practical performance load and accepted M12 as flawless: no latency, glitches, distortion, workspace/transport issues, fader faults, or disconnection faults were reported.

Manual audio confidence check:
- Begin with conservative headphone/speaker volume.
- Distribute active recognisable channels across X -2 through +2 and confirm stereo separation/travel while peers remain stable.
- Replot the same X across different Y positions and confirm Y is audibly neutral.
- Exercise Live Trim +16, negative attenuation, mute and reset 0; confirm Threads programmed level never changes.
- Exercise Visualiser Play/Pause/Resume/Stop/Play and confirm inactive sources remain inactive with no duplication or accumulated loudness.
- Navigate Visualiser -> Sound Desk -> Threads while live-editing X/trim and confirm one coherent session.
- Deliberately exercise rapid pan, trim, mute and transport transitions and listen for clicks, pops or transient spikes.

10. ENVIRONMENT / DEPENDENCIES

- Runtime: Node.js >=22.13.0.
- Package manager: pnpm 11.16.0, locked by `pnpm-lock.yaml` and `packageManager`.
- UI/runtime: React 19.2.6, React DOM 19.2.6, vinext 1.0.0-beta.2, Vite 8.0.13.
- Language/tooling: TypeScript 5.9.3, ESLint 9.39.4.
- Browser QA: Playwright and @axe-core/playwright; Chromium required.
- Audio: browser-native Web Audio API; no new audio package or service dependency.
- Build: Cloudflare Worker-compatible ESM through the existing vinext/Vite configuration.
- Native build allowlist remains esbuild, sharp and workerd only.
- No secrets or external services are required.
- Development: `pnpm dev`, normally `http://localhost:3000`.
- Production preview: `pnpm build`, then `pnpm start` (quality gate uses port 3002).

11. NEXT INTENDED BATCH

Milestones 12 and 13 passed user listening/interaction acceptance. On 2026-08-15 the user explicitly authorised this complete batch to become the new GitHub baseline.

Before any next milestone or publication:
- Read this handover and `milestone_notes/milestone_12_sound_desk_multi_plot.txt` completely.
- Verify repository state from Git rather than folder names and preserve the M13 hard-save lineage.
- Preserve source/endpoint separation, Duplicate independence, phase-coherent `1 / N` family normalization, debounced recording-safe insertion, native output fencing, endpoint smoothing, and non-destructive transport.

12. INSTRUCTIONS FOR THE NEXT CODING AGENT

A new coding agent must:
- Read `HANDOVER.txt` and the Milestone 12 and 13 notes before editing.
- Inspect repository state rather than assuming this handover is perfectly current.
- Preserve approved interaction, routing, audio isolation and cleanup contracts.
- Prefer minimal targeted changes over broad refactoring.
- Run verification in proportion to risk and report baseline failures before attempting broad repairs.
- Never claim automated browser tests prove human audible quality.
- Do not begin M14 or later roadmap work without an explicit new brief.

13. BASELINE FINGERPRINT

Milestone: 07 — Single-Channel Audible Signal Test (combined approved Milestones 06–07 batch)
Status: COMPLETE — user-approved
Previous approved commit: `2bc63fc` (Milestones 06–07 handover boundary)
Repository source branch: `agent/milestone-09`
Remote project branch: `milestone/ui-interaction-harness`, Milestone 09 merge `5d5be82`
Approved-baseline verification: PASS — typecheck, lint, build, 34/34 Node tests, 10/10 Playwright tests, axe, user acceptance
Approved-baseline audio model: one shared application runtime/master plus isolated CH 01 source/gain/analyser instance
Local development state: combined Milestones 08–09 implementation complete, user-approved, and promoted as the Milestone 09 baseline
Local verification: PASS — typecheck, lint, build, 44/44 Node tests, 13/13 Playwright tests and axe
Local audio model: independent source/envelope/programmed-gain/trim/analyser/panner branches -> one shared session gate/master
Next work: user testing and explicitly requested M10 feedback/bug fixes only; do not begin M11
Handover updated: 2026-08-12 (Europe/London)
MILESTONE 10 UPDATE — 2026-08-12

Source replication now has two explicit ownership modes. Clone uses one shared oscillator/source signal with independent routed spatial/performance endpoints; Duplicate copies programming into a new independent source/channel identity. Clone source programming is read-only in Inspector and directs edits back to the root source. Root deletion is cascade-protected whenever dependent Clone paths exist. Clone families use equal-power endpoint normalisation to preserve master headroom. See milestone_notes/milestone_10_source_replication.txt for the implemented contract and verification boundary.

Channel Out destinations are no longer locked to matching source numbers. A source endpoint such as CH 04 can connect to any free Channel Out, while a one-route-per-endpoint invariant prevents accidental fan-out outside the explicit Clone operation. Sound Desk routability follows the source endpoint identity rather than the destination slot number.

END-OF-DAY LOCAL CANDIDATE BOUNDARY — 2026-08-12

- APPROVED REPOSITORY BASELINE: Milestone 09 at `9af3b8d` on GitHub/default branch `milestone/ui-interaction-harness`.
- LOCAL M10 WORKSPACE: `/Users/requiredrequired/Documents/ChatGPT/Test Engine. Milestones 03-06`.
- LOCAL M10 BRANCH: `local/milestone-10-candidate`, created from `9af3b8d`; no remote branch and no M10 commit.
- M10 STATUS: implemented locally, unapproved, and under user testing.
- M10 PROMOTION: not permitted until explicit user approval.
- EXPECTED NEXT WORK: manual testing and user-directed M10 feedback/bug fixes only.
- M11 STATUS: do not begin.
- LOCAL REVIEW URL: `http://localhost:3000/`.
- RESUME COMMAND: from the local M10 workspace, run `pnpm run dev`; if the shell cannot find Node, prepend `/Users/requiredrequired/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin` and `/Users/requiredrequired/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback` to `PATH`.
- PRESERVATION: retain browser/local application state, dependencies, caches, generated development resources, and all uncommitted M10 files. Do not reset this workspace to M09 merely because GitHub remains on M09.

MILESTONE 10.1 LOCAL SAFETY PATCH — 2026-08-13

- STATUS: critical M10.1 output-safety work is implemented only in the local M10 candidate and remains unapproved/unpromoted.
- ENGINE BOUNDARY: one shared AudioContext/session gate/master now feeds one authoritative AudioWorklet sample-safety processor, one post-protection analyser, then the sole device connection.
- POLICY: limiting begins at -9 dBFS with 12:1 reduction, a deterministic -6 dBFS sample ceiling, 120 ms release, 1.5-second peak hold, and 30 Hz UI reports. These are configurable commissioning values, not physical safety guarantees.
- METER: Threads Monitor reports measured post-protection sample peak/hold/reduction and NORMAL, CAUTION, LIMITING, OVERLOAD, or SAFETY MUTE. It never labels digital values SPL or true peak.
- SAFETY MUTE: non-finite audio latches silence inside the worklet until explicit reset. Non-finite frequency/gain/trim/pan inputs retain valid engine state.
- WAVEFORM: the Monitor oscilloscope now taps the final analyser after protection; it no longer normalises separate upstream channel traces into a visual composite.
- COMMISSIONING RANGE: 50 Hz–10 kHz, explicitly labelled as a development/commissioning range and not a safety limit.
- DOCUMENTS: see `SAFE_LISTENING_CHARTER.md` and `milestone_notes/milestone_10_1_output_safety.txt` for graph, rationale, fixtures, manual low-level acceptance and limitations.
- NEXT WORK: independent review and conservative user acceptance of M10/M10.1 only. Do not promote and do not begin M11.

MILESTONE 11 SYSTEM QUALIFICATION — 2026-08-13

- STATUS: automated qualification PASS; independent review and conservative manual acceptance remain outstanding. M10/M10.1/M11 is still an unapproved candidate.
- START: `review/m10-safety-audit` at `096f5aeb52d4028410174d0c970e7a7e485d1125`, cleanly descended from approved M09 `9af3b8d5d64f8a1bdfbb34f01828c2003a24b314`.
- FIXTURE: 25 routed endpoints, 21 source families/oscillators, one root + four-Clone family, one root + four independent Duplicates, and fifteen additional independent sources.
- STRESS: deterministic movement/routing/trim/transport churn, active deletion, return-to-one/zero/rebuild, three repeated lifecycle cycles, stereo peak, 10×/100× sustained finite overload, invalid controls, reset acknowledgement, MessagePort recreation, and fail-closed worklet creation all pass.
- BROWSER: 25/25 Sound Desk points, 25 sounding endpoints, 21 active sources, singular context/master/session gate/safety path, disconnect/reconnect recovery, one Monitor canvas, finite measured safety state after main-thread pressure, and no captured page/console errors.
- DEFECTS FIXED: truthful RESET REQUESTED worklet acknowledgement; empty source-record cleanup; explicit MessagePort handler cleanup; invalid spatial Live Trim rejection; non-interactive canvas legend no longer intercepts dense Channel Out clicks.
- VERIFICATION: TypeScript PASS; ESLint PASS; production build PASS; 76/76 Node tests PASS; 16/16 Playwright tests PASS; axe serious/critical PASS; `git diff --check` PASS.
- DETAIL: `milestone_notes/milestone_11_system_qualification.txt` contains the full fixture, results, performance observations, defect matrix, limitations, and conservative manual test.
- BOUNDARY: do not merge, promote, modify the approved M09 branch, or begin M12. Await independent review, user manual acceptance, and explicit approval.

M10.1 / M11 SAFARI DEFECT-ISOLATION LOG — 2026-08-13 (SUPERSEDES EARLIER STATUS WORDING)

Final local status:
- The user completed manual acceptance of the M10/M10.1/M11 candidate on 2026-08-13 after the clean native-fence listening passes described below.
- Clean-runtime qualification and fast-forward promotion succeeded on 2026-08-14. `milestone/ui-interaction-harness` is authoritative through M11. Do not begin M12 without a new explicit brief.
- Intended product direction is offline/local. Maintaining a parallel Safari production baseline is not a core objective. Safari was used here as a diagnostic host; the eventual offline audio boundary remains a later decision.

Evidence and reported symptoms:
- User voice recording: `/Users/requiredrequired/Desktop/Test 01 RFE sound crackle.m4a`.
- User screen recording: `/Users/requiredrequired/Pictures/Screen Recording 2026-08-13 at 19.20.10.mov`.
- Safari produced cursor-correlated crackle/sputter, then progressive approximately 0.5–1 second latency, most visibly on Play/Pause/Stop. The same local servers did not reproduce the bad sound in the ChatGPT native browser.
- Opening Safari's blocking native channel-removal confirmation produced a separate clip/global interruption: unrelated channels still appeared active but audio required channel and transport toggles to recover.

Reduction sequence and conclusion:
1. `full`: custom AudioWorklet protection, meter reporting, and waveform observation present; Safari fault reproduced.
2. `no-scope`, `no-meter`, and `no-observers`: individual observation layers removed for comparison.
3. `minimal-fence`: limiter/meter calculations and observation removed while the custom AudioWorklet boundary remained. Progressive Safari latency still developed.
4. `native-fence`: custom AudioWorklet removed; native DynamicsCompressorNode plus WaveShaper sample fence retained; waveform and meter reporting disabled. Safari remained responsive with no crackle or latency.
5. The blocking `window.confirm()` channel-removal flow was replaced with an inline non-blocking warning. Deleting a channel mid-session then preserved unrelated audio and transport state.
6. `native-fence-scope`: endpoint analyser animation restored without restoring the worklet or MessagePort reports. Threads, transport, channel deletion, Sound Desk live plotting, and changing plots while sounding were manually accepted without crackle, interruption, latency, or dysfunctional controls.
7. Monitor presentation was then changed at user request from one summed output trace to one colour-coded trace per sounding endpoint. Traces are slightly vertically separated and sampled at a bounded 30 Hz visual rate. This final presentation was manually accepted with no additional issue.

Suspended elements and rationale:
- CUSTOM AUDIOWORKLET IN THE ACCEPTED LISTENING PATH: suspended because it was the common remaining boundary when Safari latency persisted in `minimal-fence`, and removing it in `native-fence` removed the fault. This isolates the integration/path as the leading cause; it does not prove a specific WebKit internal mechanism.
- WORKLET MESSAGEPORT MASTER METER: suspended with the worklet. Current/held dBFS, reduction, safety-state reports, non-finite SAFETY MUTE reason, and acknowledged reset are therefore not active in the accepted listening configuration.
- WORKLET NON-FINITE LATCH: suspended in the accepted listening configuration. The native comparison fence must not be represented as equivalent pathological-input protection or as a true-peak/hardware-safety guarantee.
- POST-PROTECTION SUMMED OSCILLOSCOPE: superseded in the visible Monitor by the requested layered endpoint view. The layered traces tap endpoint analysers before pan, summing and final protection; they show contributions, not the exact hardware-bound composite waveform. The final analyser still exists in the graph but is not the source of the layered display.
- CROSS-BROWSER PARITY: deferred because the intended application is offline/local and the user explicitly declined making a parallel Safari baseline a core objective.

Preserved for later investigation:
- The custom AudioWorklet implementation, safety reports, reset acknowledgement, non-finite latch, synthetic overload tests, and diagnostic URL modes remain in source; they were not deleted.
- No automatic unprotected fallback was introduced. Native modes are selected explicitly by the diagnostic URL.
- The accepted local URL is `http://localhost:3002/?audioDiagnostic=native-fence-scope`.
- A future audio-boundary decision must separately reconcile deterministic pathological-input handling, truthful final-output metering, the layered contribution view, offline desktop runtime constraints, and the requirement not to reintroduce audible latency or crackle.

Deletion-warning repair:
- Root cause boundary: Safari's blocking page-level `window.confirm()` interrupted the active audio experience independently of the worklet latency issue.
- Repair: an app-owned inline alert with explicit CANCEL and REMOVE actions; opening/cancelling it does not mutate the audio runtime, while confirmation disposes only the addressed source/channel.
- Regression coverage verifies warning open, cancel, and confirm preserve an unrelated sounding channel and `playing` transport state.

Resume/preservation:
- Workspace after neutral-name housekeeping: `/Users/requiredrequired/Documents/ChatGPT/RFE-Test-Engine-Dev`.
- Authoritative branch: `milestone/ui-interaction-harness`; review evidence remains on `review/m10-safety-audit`.
- Review URL: `http://localhost:3002/?audioDiagnostic=native-fence-scope`.
- Restart from the workspace with `/Users/requiredrequired/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node node_modules/vinext/dist/cli.js start --port 3002` after running the corresponding `build` command if required.
- Final wrap verification: TypeScript PASS; ESLint PASS; production build PASS; 78/78 Node tests PASS; 18/18 Playwright tests PASS including axe; `git diff --check` PASS.

RUNTIME HYGIENE — PERMANENT RULE

Git/project state persists; local runtime state does not.

End of a development session:
- stop every RFE localhost server;
- close/discard localhost application and automated test tabs;
- discard long-lived browser, AudioContext and hot-reload runtime state.

Start of a development session:
- verify repository root, branch, HEAD and approved/candidate status from Git;
- start exactly one fresh server from that verified root;
- open a fresh browser context before development or acceptance testing.

Hard-save qualification must always use a freshly started server and fresh browser context. Never qualify a release using browser/server state that survived earlier builds, branch changes or development days. The 2026-08-14 clean-runtime rerun confirmed this rule: two stale servers produced duplicated/missing UI behavior, while one fresh server restored the 18/18 browser result.

REPOSITORY NAMING CONSISTENCY — PERMANENT RULE

Before hard saves and new milestone contexts verify:
- the local workspace name is neutral/current;
- the authoritative branch corresponds to the actual approved baseline;
- review branches clearly identify unapproved candidates;
- milestone notes correspond to the milestones they document;
- stale milestone-labelled directories are not treated as authoritative;
- handover identifies any mismatch between workspace name, branch, approved milestone and candidate milestone.

Authoritative project state is determined by Git ancestry, commit SHA and documented approval. Folder names do not determine project state.

M11 BASELINE FINGERPRINT — 2026-08-14 09:02:44 BST

- Approved through: M11.
- Authoritative branch: `milestone/ui-interaction-harness`.
- Qualified implementation SHA: `7cf92aa7b61b9363d67eb332275f1a258c5c103c`.
- Previous approved M09 SHA: `9af3b8d5d64f8a1bdfbb34f01828c2003a24b314`.
- Verification: TypeScript PASS; ESLint PASS; production build PASS; Node/model 78/78 PASS; fresh-runtime Playwright 18/18 PASS including axe; Git diff integrity PASS.
- Architecture: one project lineage; Clone/Duplicate ownership; decoupled Channel Out routing; shared context/session/master path; accepted `native-fence-scope` listening mode; layered endpoint traces; non-blocking deletion confirmation.
- Known limitations: native fence is not true peak or SPL; worklet meter/non-finite latch is suspended in the accepted listening mode; layered traces are pre-pan/pre-sum endpoint views rather than the final hardware-bound composite; final offline desktop audio boundary remains a future decision.
- Runtime target: offline desktop first; Tablet Compact second; browser build is a development/prototyping runtime.
- M12 candidate: implemented locally on `agent/milestone-12`; M11 remains the authoritative promoted baseline until explicit M12 approval.

M12 ACCEPTED LOCAL CANDIDATE FINGERPRINT — 2026-08-14

- Status: USER-ACCEPTED, LOCAL, UNCOMMITTED, UNPROMOTED.
- Candidate branch: `agent/milestone-12`; base/HEAD before candidate changes: `a54e9599e10080a8ad4b325ec78c316faf2f48fd`.
- Product change: Threads-facing Clone creation is retired; Sound Desk Multi-Plot creates additional independently positioned/muted/trimmed endpoint branches for an existing Channel Out.
- Audio ownership: Multi-Plots reuse the M10 shared-source family graph, keep one oscillator/source envelope, and retain `1 / sqrt(N)` endpoint family normalization. Duplicate remains an independent source/oscillator.
- Live drag: pointer-captured, requestAnimationFrame-limited panner modulation with the existing 20 ms AudioParam ramp; no graph reconstruction and no hot-path runtime subscriber notification.
- Future modes: compact Plotter drawer exposes disabled AUTO PLOT and ORCHESTRA placeholders only.
- Verification: see `milestone_notes/milestone_12_sound_desk_multi_plot.txt` for the complete candidate qualification and manual acceptance steps.
- Boundary: user authorised M13 without promoting M12. Do not merge, promote, alter M11 Git authority, or begin M14/later roadmap work without explicit approval.

M13 BASELINE FINGERPRINT — 2026-08-15

- Status: USER-ACCEPTED, MERGED, AND PROMOTED.
- Implementation commit: `3be2f6e9f94b6946facefc60825559fcefb56b87`; merged to `milestone/ui-interaction-harness` by GitHub PR #2 as `cec46def328b8341f87222b84ffc903eac1fb7bc`. Previous M11 hard-save HEAD: `a54e9599e10080a8ad4b325ec78c316faf2f48fd`.
- Product change: one reusable finite pitch instruction now drives the Sine, Triangle, and Saw pitched-generator family through the established lifecycle and routing graph.
- Ownership: Duplicate copies generator programming then remains independent; Multi-Plot retains one source oscillator and independent spatial endpoints.
- Small edits: module port targets are 150% of configured size, live Multi-Plot insertion is deliberately staged for recording safety, and Sound Desk exposes an explicit sounding/total/source tally.
- Post-trial remediation: unchanged Live Trim synchronization no longer cancels the 60 ms live-branch insertion fade; multiply occupied coordinates use a centred visual fan with explicit stack count/position while retaining identical audio coordinates.
- Second post-trial remediation: live family rebalancing now shares the 60 ms insertion transition across existing and new branches; stacks of three or more open as a Plot Point Folder with explicit FRONT/LAYER/BACK ordering controls.
- Third post-trial remediation: shared-source endpoints are phase-coherent, so M13 now uses `1 / N` family amplitude normalization instead of the uncorrelated-signal `1 / sqrt(N)` law inherited from M12. Successive live insertions therefore preserve constant summed family gain while the coordinated 60 ms crossfade runs.
- Fourth post-trial remediation: user listening found the coherent normalization softened but did not eliminate the transient. Live insertion now holds the newly connected branch and the existing family automation unchanged for 10 ms before the coordinated 60 ms crossfade, isolating the browser's graph mutation across several silent render quanta.
- Fifth post-trial remediation: analysis of the valid original iPad recording showed a continuous 84 Hz Triangle carrier beneath prominent mechanically captured click impulses. The user explicitly prioritised clean live recording over clone delivery speed, so insertion now stages the branch silently for one full second and then uses a coordinated 250 ms zero-slope smoothstep crossfade. The existing family remains unchanged throughout the silent staging interval.
- Sixth post-trial remediation: user testing exposed temporary amplitude doubling when additions were made faster than the one-second stage. Independent delayed curves could overlap: an earlier clone began entering after a later click had postponed family regulation. Live additions now debounce per source family into one atomic transaction. Every rapid click resets one timer, all pending endpoints remain at zero, delivered endpoints keep their current normalization, and one shared smoothstep crossfade begins only after a full quiet second. A 65-point regression proves summed family gain never exceeds its pre-click value during a two-clone batch.
- Seventh post-trial remediation: the 3+ Plot Point Folder marker is now a drag handle for its complete co-located cluster. Cluster movement is one atomic spatial-state action and previews every endpoint panner together. It does not bind membership: any folder endpoint can still be selected and routed to another point individually, and REMOVE continues to delete only the chosen Multi-Plot. Model coverage and the production browser journey prove cluster move, individual departure, and independent deletion in sequence.
- Eighth post-trial remediation: an individual can now be pulled free without the unplot/find/replot detour. Double-click any folder-list member to expose that endpoint as an immediately draggable live plot; the selected member can also be pulled with a double-click on the grid folder marker, and the marker defaults to the front endpoint when none is selected. The route remains live, the remaining occupants stay clustered, and the normal drag workflow commits the detached endpoint to its new coordinate. Browser coverage proves a selected list member can leave a moved three-endpoint cluster independently.
- Verification: see `milestone_notes/milestone_13_musical_source_architecture.txt` for the full qualification and manual musical acceptance procedure.
- Acceptance: the final user test found the workflow responsive and user-friendly, with no bad noise or misbehaving functions. M12–M13 is the authoritative GitHub baseline. Do not begin M14 without a new explicit brief.
